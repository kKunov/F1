each team in teams
    .row
        .col-xs-2.col-xs-offset-2
                img.img-thumbnail(src=team.pic)
                h5.text-center= team.name

        .col-xs-2.col-xs-offset-1
            h2= team.engine

        each driver in team.drivers
            .col-xs-1
                img.img-thumbnail(src=driver.pic)
                h6.text-center= driver.name
